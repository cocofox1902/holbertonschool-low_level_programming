#include "main.h"

/**
 * _islower - lower case send 1
 *
 * @c: variable
 *
 * Return: 1 is lower case 0 is upper case
 */

int _islower(int c)
{
	if (c > 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
